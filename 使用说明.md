# 强提醒（Mac）

一个离线的中文定时提醒工具。到点后在所有显示器上覆盖全屏，并每两秒响一次系统提示音，直到点“我知道了”或“5 分钟后再提醒”。支持 Apple Silicon 和 Intel Mac。

## 安装和使用

1. 打开 `强提醒-Mac.dmg`，把 `强提醒.app` 拖到“拖到这里安装”。
2. 打开“强提醒”，填写提醒内容和日期时间，点“添加提醒”。
3. 可点“预览全屏效果”检查显示和音量。
4. 建议勾选“登录时自动启动”；若系统要求批准，在“系统设置 → 通用 → 登录项与扩展”中允许。
5. 关闭主窗口后，程序继续在菜单栏运行；点菜单栏的闹钟图标可重新打开。退出程序后提醒不会触发。

提醒只触发一次。程序会在唤醒或重新启动时检查错过的时间；超过预定时间 1 小时的提醒会被清除。Mac 关机或程序未运行时无法弹窗；登录后自动启动须由用户启用。锁屏界面不能保证显示全屏提示。响铃音量随系统声音设置。

提醒数据只保存在本机的 `~/Library/Application Support/强提醒/alarms.json`，程序不使用网络，也不读取日历。

这是本机临时签名的应用，没有 Apple 开发者公证。若 macOS 首次打开时拦截，可在“系统设置 → 隐私与安全性”中允许打开。

## 从源码构建

需要 macOS 13 或更新版本，以及 Xcode Command Line Tools。在源码目录执行 `./run-tests.sh` 和 `./build.sh`。
